

const style {
    wrapper: `w-min flex items-center`,
}

function SidebarOptions({ text, Icon, isActive, setSelected, redirect }) {
  return (
    <div>
      
    </div>
  );
}

export default SidebarOptions;
